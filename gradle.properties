# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
# Set the Kotlin compiler execution strategy to in-process to avoid "Could not
# connect to Kotlin compile daemon" error.
kotlin.compiler.execution.strategy=in-process
# Allow the google-services plugin to work even if the google-services.json is not present.
googleServices.missing.passthrough=true
# org.gradle.java.home is deliberately NOT set. It used to pin an absolute path
# to one Mac's Temurin 17 install, which meant the repo could not build on any
# other machine — or in CI — without editing a tracked file. The
# foojay-resolver-convention plugin in settings.gradle.kts provisions a
# toolchain instead, which is what it is for.

# Patched floors for libraries the build tools bring in (root build.gradle.kts
# explains). Comma-separated group:name:version.
buildToolFloors=org.bouncycastle:bcprov-jdk18on:1.86,org.bouncycastle:bcpkix-jdk18on:1.86,org.bouncycastle:bcutil-jdk18on:1.86,org.jdom:jdom2:2.0.6.1,org.bitbucket.b_c:jose4j:0.9.7,org.apache.commons:commons-lang3:3.20.0,com.google.guava:guava:32.1.3-android,org.apache.httpcomponents:httpclient:4.5.14
